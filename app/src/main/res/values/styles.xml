<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorWhite</item>
        <!-- <item name="colorPrimaryDark">@color/colorPrimaryDark</item> -->
        <!-- <item name="colorPrimary">@color/colorWhite</item> -->
        <item name="colorPrimaryDark">@color/colorMain</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="MenuText">
        <item name="android:textColor">@color/colorMain</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Toolbar" parent="AppTheme">
        <item name="android:textColorPrimary">@color/colorMain</item>

        <item name="android:textColorSecondary">@color/colorMain</item>

    </style>

    <style name="BottomTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorMain</item>
        <!-- <item name="colorPrimaryDark">@color/colorPrimaryDark</item> -->
        <!-- <item name="colorPrimary">@color/colorWhite</item> -->
        <item name="colorPrimaryDark">@color/colorMain</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="Bottom_nav" parent="BottomTheme">
        <item name="android:textColorPrimary">@color/colorMain</item>

        <item name="android:textColorSecondary">@color/colorMain</item>
        <item name="android:textColor">@color/colorMain</item>
        <item name="android:colorControlNormal">@color/colorMain</item>
        <item name="android:colorControlHighlight">@color/colorMain</item>
    </style>


    <style name="popupTheme" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="colorPrimary">@color/colorMain</item>
        <item name="colorPrimaryDark">@color/colorMain</item>
        <item name="colorAccent">@color/colorMain</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

</resources>
